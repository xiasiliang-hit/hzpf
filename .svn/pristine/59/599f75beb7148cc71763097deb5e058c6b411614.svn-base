package com.line.bqxd.platform.v2.login;

/**
 * Created by huangjianfei on 16/9/8.
 */
public class FilterUtils {
    /*
    public static ActivityDomain getActivityDomain(HttpServletRequest request, ActivityManager activityManager,Logger logger) {
        long activityId = 0;
        long shareUserId = 0;
        long shareTextId = 0;
        long channelId = 0;
        //分享的层次
        int shareLevel = 0;
        String from = ActivityDomain.TIME_LINE_FROM;
        long concurPlanId = 0;
        long concurPlanGruopId = 0;

        String activityIdStr = request.getParameter("activityId");
        String shareUserIdStr = request.getParameter("shareUserId");
        String shareTextIdStr = request.getParameter("shareTextId");
        String channelIdStr = request.getParameter("channelId");
        String shareLevelStr = request.getParameter("shareLevel");
        String fromStr = request.getParameter("f");
        String concurPlanIdStr = request.getParameter("concurPlanId");
        String concurPlanGruopIdStr = request.getParameter("concurPlanGruopId");
        try {
            if (StringUtils.isNotBlank(activityIdStr)) {
                activityId = Long.parseLong(activityIdStr);
            } else {
                activityId = activityManager.getActivityDO().getDefaultActivityId();
            }
            if (StringUtils.isNotBlank(shareUserIdStr)) {
                shareUserId = Long.parseLong(shareUserIdStr);
            }
            if (StringUtils.isNotBlank(shareTextIdStr)) {
                shareTextId = Long.parseLong(shareTextIdStr);
            }else{
                shareTextId=activityManager.getActivityDO().getDefaultShareTextId();
            }
            if (StringUtils.isNotBlank(channelIdStr)) {
                channelId = Long.parseLong(channelIdStr);
            }else{
                channelId=activityManager.getActivityDO().getDefaultChannelId();
            }
            if (StringUtils.isNotBlank(shareLevelStr)) {
                shareLevel = Integer.parseInt(shareLevelStr);
            }

            if (StringUtils.isNotBlank(fromStr)) {
                from = fromStr;
            }
            if (StringUtils.isNotBlank(concurPlanIdStr)) {
                concurPlanId = Long.parseLong(concurPlanIdStr);
            }
            if (StringUtils.isNotBlank(concurPlanGruopIdStr)) {
                concurPlanGruopId = Long.parseLong(concurPlanGruopIdStr);
            }
        } catch (Exception e) {
            logger.error("getActivityDomain is error", e);
        }
        ActivityDomain activityDomain = new ActivityDomain(activityId, shareUserId, shareTextId, channelId, shareLevel, from);
        activityDomain.setConcurPlanId(concurPlanId);
        activityDomain.setConcurPlanGruopId(concurPlanGruopId);
        //sessionUserInfo.setExtData(activityDomain);
        return activityDomain;
    }
*/
}
